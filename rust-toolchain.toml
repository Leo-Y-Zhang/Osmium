[toolchain]
# Pinned: the bootloader crate's image builder needs nightly cargo (-Zbuild-std).
# The kernel itself stays stable-clean — CI enforces that with a stable build job.
# Bump this pin only in a dedicated PR.
channel = "nightly-2026-08-01"
components = ["rust-src", "llvm-tools-preview", "rustfmt", "clippy"]
targets = ["x86_64-unknown-none"]
