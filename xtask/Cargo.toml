[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
# Exact pin: the bootloader build script installs its boot-stage binaries at
# this same version, so any skew between the two breaks image creation.
bootloader = "=0.11.17"
